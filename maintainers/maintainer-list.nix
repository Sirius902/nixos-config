# Maintainers not (yet) in nixpkgs' own maintainers/maintainer-list.nix,
# merged into `lib.maintainers` by overlays/default.nix. Same schema as
# nixpkgs: required `name`, `github`, `githubId`; optional `email`, `matrix`,
# `keys`. Keep alphabetically sorted, and drop an entry once it lands upstream.
{
  sirius902 = {
    name = "Sirius902";
    github = "Sirius902";
    githubId = 10891979;
  };
}
